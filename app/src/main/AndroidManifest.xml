<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name="es.unizar.eina.parcelapad.ui.parcelas.ParcelaEdit"
            android:exported="false" />
        <activity
            android:name="es.unizar.eina.parcelapad.ui.parcelas.Parcelapad"
            android:exported="false" />
        <activity
            android:name="es.unizar.eina.reservapad.ui.reservas.ReservaEdit"
            android:exported="false" />
        <activity
            android:name="es.unizar.eina.reservapad.ui.reservas.Reservapad"
            android:exported="false" />
        <activity android:name="es.unizar.eina.welcome.WelcomeActivity"
            android:exported="true">
            <!-- Establece esta actividad como la principal -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>