<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/big_padding">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/lista_parcelas_todas"
        android:textSize="18sp"
        android:layout_marginBottom="@dimen/small_padding" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/border">

        <LinearLayout
            android:id="@+id/listaParcelasContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <!-- Las filas de parcelas se agregarán dinámicamente aquí -->
        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/button_save_add_parcela"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/button_save_add_parcela"
        android:layout_marginTop="@dimen/small_padding"
        android:background="@color/colorPrimary"
        android:textColor="@color/buttonLabel" />
</LinearLayout>
